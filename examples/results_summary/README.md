# Verified result summaries

These compact files come from the completed analysis and are included so users can inspect key outputs without downloading large intermediates.

Metadata-derived labels have been publication-curated where explicitly documented. In particular, PRJNA285775 is relabeled from the archive-derived `WGS` field to `RAD-Seq` based on Qiu et al. (2016). This correction changes metadata grouping/wording but does not alter the underlying FASTQ reads, mappings, MAPQ query-name sets, or downstream variant/depth measurements already computed for those runs. The six runs previously selected under the mislabeled `wgs_contrast` group are the same six selected by the corrected PRJNA285775 SbfI RAD-seq contrast rule; only the scientific label/reason changes.

The invalid WGS-versus-RAD rows are intentionally removed from `silene_technical_group_comparisons_curated.tsv`.
